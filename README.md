# media-forge
CLI based batch media converter for Linux. Convert various image type to a better compression format with minimum quality loss (AVIF, HEIF, WEBP). Also support video encoding to AV1 using FFMPEG with CUDA acceleration.
